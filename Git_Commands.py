"""

# git branch -> used to List the branches in your repository.

# git branch -a -> List all branches (local and remote).

# git checkout <branch_name> -> Switch to the specified branch.

# git checkout -b <branch_name> -> Create a new branch.

# git branch -d <branch_name> -> Delete the specified branch.

# git branch -D <branch_name> -> Force delete the specified branch.  


# git merge <branch_name> -> Merge the specified branch into the current branch.


"""